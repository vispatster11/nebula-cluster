# DEPRECATED
# Use `.github/workflows/ci-combined.yml` for the combined pipeline
# Or run the independent workflows in `.github/workflows/` (python-quality, image-scan, helm-lint, integration-tests)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "This workflow is intentionally deprecated."
          echo "See .github/workflows/ci-combined.yml or run the independent workflows instead."
